# NvimConfig12

- Configuration for neovim version 0.12+, using native `vim.pack`
- `plugin/wsl.lua` defines WSL specific configuration especially for clipboard
    - remove it in other environment
- `plugin/ssh-tmux.lua` defines tmux specific configuration especially for clipboards
    - remove it in other environment
